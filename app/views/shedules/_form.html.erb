<%= form_with model: @tour, local: true do |form| %>

  <% if @shedule.errors.any? %>
    <div id="error_explanation">
      <h4 style="color: #B00100">
        <%= pluralize(@shedule.errors.count, "error") %> prohibited this article from being saved:
      </h4>
      <ul>
        <% @shedule.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
  </div>

  <div class="form-group">
    <%= form.label :datetimetour %><br>
    <%= form.date_field :datetimetour, class: "form-control" %><br>
  </div>
  <div class="form-group">
    <%= form.label :countplace %><br>
    <%= form.number_field :countplace, class: "form-control" %><br>
  </div>
  <div class="form-group">
    <%= form.label :tourcost %><br>
    <%= form.text_field :tourcost, class: "form-control" %><br>
  </div>
  <div class="form-group">
    <%= form.label :tour_id %><br>
    <%= form.number_field :tour_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :tour_id %><br>
    <%= form.collection_select(:tour, Tour.all, :id, :title) %>
  </div>

  <div class="form-group">
    <%= form.submit :class => "btn btn-primary" %>
  </div>

<% end %>
