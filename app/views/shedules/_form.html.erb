<%= form_with model: @shedule, local: true do |form| %>

  <% if @shedule.errors.any? %>
    <div id="error_explanation">
      <h4 style="color: #B00100">
        <%= pluralize(@shedule.errors.count, "error") %> prohibited this sheduleitem from being saved:
      </h4>
      <ul>
        <% @shedule.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <div class="form-group">
  </div>

  <div class="form-group">
    <%= form.label :datetimetour %><br>
    <%= form.datetime_field :datetimetour, class: "form-control date_time_picker", input_html:
        {data:
             {
                 date_format: "YYYY-MM-DD HH:mm",
             }
        } %>
  </div>


  <%#= form.datetime_field :when, as: :date_time_picker %>

  <div class="form-group">
    <%= form.label :countplace %><br>
    <%= form.number_field :countplace, class: "form-control" %><br>
  </div>
  <div class="form-group">
    <%= form.label :tourcost %><br>
    <%= form.text_field :tourcost, class: "form-control" %><br>
  </div>

  <div class="form-group">
    <%= form.label :tour_id %><br>
    <%#= form.collection_select :tour, Tour.all,:id,:title, {}, {class: 'form-control'} %>
    <%= form.collection_select :tour_id, Tour.all, :id, :title, {prompt: "Выберите экскурсию"}, {class: 'form-control'} %>
  </div>

  <div class="form-group">
    <%= form.submit :class => "btn btn-primary" %>
  </div>

<% end %>