<div class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <a href="../" class="navbar-brand">TourAgency</a>
      <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navbar-main">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="navbar-collapse collapse" id="navbar-main">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to 'Экскурсии', controller: 'tours' %>
        </li>
        <li>
          <%= link_to 'Добавить экскурсию', new_tour_path %>
        </li>
        <li>
          <%= link_to 'Добавить запись в расписание', new_shedule_path %>
        </li>
        <li>
          <%= link_to 'Запись на экскурсию', new_tour_path %>
        </li>
        <li>
          <%= link_to 'Расписание экскурсий', shedules_path %>
        </li>
      </ul>
    </div>
  </div>
</div>
<div class="container">
  <div class="page-header" id="banner">
    <div class="row">
      <div class="col-lg-12">
        <h1>Расписание экскурсий</h1>
        <table class="table table-hover table-bordered">
          <tr>
            <th>Название</th>
            <th>Дата</th>
            <th>Количество мест</th>
            <th>Стоимость, р</th>
            <th></th>
            <th></th>
            <th></th>
          </tr>
          <% @shedules.each do |shedule| %>
            <tr>
              <td><%= shedule.tour.title %></td>
              <td><%= shedule.datetimetour %></td>
              <td><%= shedule.countplace %></td>
              <td><%= shedule.tourcost %></td>
              <td><%= link_to 'Показать', shedule_path(shedule) %></td>
              <td><%= link_to 'Редактировать', edit_shedule_path(shedule) %></td>
              <td><%= link_to 'Удалить', shedule_path(shedule),
                              method: :delete,
                              data: {confirm: 'Вы уверены?'} %></td>
            </tr>
          <% end %>
        </table>
      </div>
    </div>
    <div class="col-lg-4 col-md-5 col-sm-6">
    </div>
  </div>
</div>